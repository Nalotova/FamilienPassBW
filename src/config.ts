// Google Maps API key configuration
// For MVP, you can paste your API key here.
// For public release, make sure to restrict this key by HTTP Referer in Google Cloud Console
// or use the proxy routes defined on the server side to protect it.
export const GOOGLE_MAPS_API_KEY = "PASTE_API_KEY_HERE";
